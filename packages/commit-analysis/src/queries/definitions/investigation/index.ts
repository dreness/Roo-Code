import forCommit from "./for-commit"
import fullDetails from "./full-details"
import candidates from "./candidates"
import evidence from "./evidence"
import recent from "./recent"
import bySubsystem from "./by-subsystem"
import inProgress from "./in-progress"
import byConclusion from "./by-conclusion"

export const investigationQueries = [
	forCommit,
	fullDetails,
	candidates,
	evidence,
	recent,
	bySubsystem,
	inProgress,
	byConclusion,
]

export { forCommit, fullDetails, candidates, evidence, recent, bySubsystem, inProgress, byConclusion }
